```plantuml
@startuml C4_Context
!include <C4/C4_Context>

LAYOUT_WITH_LEGEND()

title Диаграмма контекста системы (Level 1) — Полиглотная система управления учебным процессом

Person(user, "Пользователь", "Формирует отчёты через API с OAuth2-аутентификацией")

System(report_system, "Система полиглотных отчётов", "Предоставляет API для генерации тестовых данных и выполнения аналитических запросов (ЛР1–3) с JWT-аутентификацией и mTLS-проксированием")

Rel(user, report_system, "Запрашивает отчёты, используя JWT токен", "HTTPS")
Rel(report_system, user, "Возвращает JSON-отчёт", "HTTPS")

@enduml
```
